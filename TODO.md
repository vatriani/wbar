A simple todo list for extra features in this program

- TODO Inefficient Workspace Sorting
    buffer.c:103–120 (sort_workspaces)
    Uses bubble sort (O(n²)) on up to 32 workspaces. Replace with qsort for O(n log n).
